{curr_loc, next_loc} = import '@fink/prattler'


unindent_text = fn text, ind:
  lines = text.split:: rx/\n/g
  unindented_lines = lines.map(fn line, idx:
    match idx:
      0: line
      else: line.slice:: ind
  )
  unindented_lines.join:: '\n'
