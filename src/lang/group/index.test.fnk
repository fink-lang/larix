{describe, it, expect, to_match_snapshot} = import '@fink/jest'

{parse_expr} = import '../..'


describe 'group: (...)', fn:

  it 'parses single line: (foo, bar)', fn:
    expect
      parse_expr '(foo, bar)'
      to_match_snapshot



