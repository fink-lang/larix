{add_non_separating} = import '@fink/prattler/symbols'

{prefix} = import '../symbols'


await_op = fn op: prefix:: op, 'await'


add_async = fn ctx:
  pipe ctx:
    add_non_separating:: await_op:: 'await'

