{add_non_separating} = import '@fink/prattler/symbols'

{prefix} = import '../symbols'


add_async = fn ctx:
  pipe ctx:
    # TODO: 'await' 'prefix' ?
    add_non_separating:: prefix:: 'await', 'await'

