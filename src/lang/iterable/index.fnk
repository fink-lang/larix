{add_named_block} = import '../block'

add_iterables = fn ctx:
  pipe ctx:
    add_named_block:: 'fold'
    add_named_block:: 'unfold'

    add_named_block:: 'map'
    add_named_block:: 'flat_map'

    add_named_block:: 'filter'
    add_named_block:: 'while'
    add_named_block:: 'find'
