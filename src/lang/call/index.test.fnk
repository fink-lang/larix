{describe, it, expect, to_match_snapshot} = import '@fink/jest'

{parse} = import '../..'


describe 'pipe foo: ...', fn:

  it 'pipes', fn:
    expect
      parse `
        pipe foo:
          bar shrub
          ni
      `
      to_match_snapshot

