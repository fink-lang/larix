{symbol: parser_symbol} = import '@fink/prattler/symbols'
{next_is_new_expr} = import './block/indentation'


symbol = fn id:
  {
    ...parser_symbol(id),

    lbp: fn lbp: fn ctx:
      match ctx:
        next_is_new_expr:: ?:
          0
        else:
          lbp
  }
